# Test-specific database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/business_test
spring.datasource.username=test_user
spring.datasource.password=test_password

# H2 in-memory database for faster tests (alternative to MySQL)
# spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# JPA settings for tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Disable features not needed in tests
spring.main.allow-bean-definition-overriding=true
management.endpoints.enabled-by-default=false

# Logging
logging.level.org.springframework=WARN
logging.level.com.business=INFO
logging.level.org.hibernate=ERROR